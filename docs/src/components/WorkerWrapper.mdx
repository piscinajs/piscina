// WorkerWrapperComponent.mdx
import React from 'react';

export const WorkerWrapperComponent = () => (
  <pre>
    <code className="language-js" title="workerWrapper.js">{`const { workerData } = require('worker_threads');

if (workerData.fullpath.endsWith(".ts")) {
require("ts-node").register();
}
module.exports = require(workerData.fullpath);
`}</code>

  </pre>
);

export default WorkerWrapperComponent;
